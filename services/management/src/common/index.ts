/**
 * Shared building blocks for the management-path services: the structured
 * domain error and an injectable clock used for deterministic timestamps.
 */
export * from './errors';
export * from './clock';
