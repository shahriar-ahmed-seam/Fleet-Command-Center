{
  "name": "@fleet/design-tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Shared design tokens (color, typography, spacing) for web and Flutter",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/generate.mjs",
    "generate": "node scripts/generate.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
